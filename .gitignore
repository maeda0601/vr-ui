# Python
__pycache__/
*.pyc
.venv/
venv/

# MediaPipe の検出モデル（約7.8MB）。uv run scripts/download_model.py で取得する
models/*.task

# コンソール無し起動時のログ
logs/

# エディタ・OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Windowsのショートカット（絶対パス・PC名が埋め込まれるため共有しない）
*.lnk

# Claude Code のローカル設定（自動コミットフックは各自の環境の都合）
.claude/
